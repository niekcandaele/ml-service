name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install UV
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        run: uv python install 3.12

      - name: Cache UV dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Run Ruff lint
        run: uv run ruff check .

      - name: Run Ruff format check
        run: uv run ruff format --check .

      - name: Run free tests
        run: uv run pytest -m "not paid" -v || test $? -eq 5

      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          # Keep Docker since we need it for building
          docker-images: false
          # Remove these large unnecessary tools
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ hashFiles('**/uv.lock') }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ml-service:${{ github.sha }}
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max
        timeout-minutes: 60

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache

  # Note: GCR push is commented out for now (requires GCP credentials setup)
  # Uncomment when hackathon GCP project is configured
  #
  # push-to-gcr:
  #   needs: test
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Authenticate to Google Cloud
  #       uses: google-github-actions/auth@v2
  #       with:
  #         credentials_json: ${{ secrets.GCP_SA_KEY }}
  #
  #     - name: Set up Cloud SDK
  #       uses: google-github-actions/setup-gcloud@v2
  #
  #     - name: Configure Docker for GCR
  #       run: gcloud auth configure-docker gcr.io
  #
  #     - name: Build and push to GCR
  #       run: |
  #         docker build -t gcr.io/${{ secrets.GCP_PROJECT_ID }}/ml-service:${{ github.sha }} .
  #         docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/ml-service:${{ github.sha }}
  #         docker tag gcr.io/${{ secrets.GCP_PROJECT_ID }}/ml-service:${{ github.sha }} gcr.io/${{ secrets.GCP_PROJECT_ID }}/ml-service:latest
  #         docker push gcr.io/${{ secrets.GCP_PROJECT_ID }}/ml-service:latest
